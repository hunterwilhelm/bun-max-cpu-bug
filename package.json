{
  "name": "bun-max-cpu",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "inspect-wait": "bun run --inspect-wait=localhost:6499/debugger --watch index.ts",
    "inspect": "bun run --inspect=localhost:6499/debugger --watch index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
